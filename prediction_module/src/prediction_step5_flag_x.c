#include "../include/prediction_module.h"

/*
 * ПРГ:Тс-7 - Общая логика обновления признака обновления прогноза X
 * Признак = 1 если F достоверна и расчёт X выполнен правильно
 * Признак = 0 в противном случае
 */

// Функция проверки для P вынесена в отдельный файл, здесь только интеграция
// Признак x_pred_updated формируется в Prediction_Run_KOI
